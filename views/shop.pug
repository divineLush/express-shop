extends layouts/base.pug

block content
    if products.length > 0
        h1 my products
        ul
            each product in products
                li
                    span #{product.title}
    else
        h1 no products
