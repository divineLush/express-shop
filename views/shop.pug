extends layouts/base.pug

block content
    if products.length > 0
        h1 my products
        ul.products
            each product in products
                li.products__item
                    span #{product.title}
    else
        h1 no products
