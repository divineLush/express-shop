extends ../layouts/base.pug

block content
    if products.length > 0
        h1 some products
        ul
            each product in products
                li
                    span #{product.title} #{product.price}$
                    p #{product.desc}
    else
        h1 no products
